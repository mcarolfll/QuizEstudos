# Quiz React

Aplicação React simples para executar um quiz com 10 perguntas sobre tecnologia/programação, com troca de tema e histórico salvo em `localStorage`.

Para rodar localmente:

```bash
npm install
npm run dev
```

Abra `http://localhost:5173` (padrão do Vite).
